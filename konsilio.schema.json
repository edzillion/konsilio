{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Konsilio Configuration",
  "description": "Configuration file for the Konsilio Council of Experts MCP server",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Path to this JSON schema file for IDE validation"
    },
    "personas": {
      "type": "object",
      "description": "Persona configuration for expert analysis",
      "properties": {
        "enabled": {
          "type": "array",
          "items": {
            "type": "string",
            "description": "Persona ID (e.g., security, performance, ux-dx, devops, or custom personas)"
          },
          "minItems": 2,
          "description": "Expert personas to include in council consultations. Must have at least 2. Built-in personas: security, performance, ux-dx, devops, typescript, graph-dba, node-fullstack, dev-tooling, distributed-systems, test-architect"
        },
        "_comment": {
          "type": "string",
          "description": "Optional comment for documentation purposes"
        }
      },
      "required": ["enabled"],
      "additionalProperties": false
    },
    "constitution": {
      "type": "string",
      "description": "Path to optional constitution file with custom workflow rules"
    },
    "models": {
      "type": "object",
      "description": "Default model configuration for all personas",
      "properties": {
        "experts": {
          "type": "string",
          "description": "Default model for expert personas. Override per-persona with personaModels."
        },
        "lead": {
          "type": "string",
          "description": "Model for consolidation phases (extraction, critique, decision, synthesis)"
        },
        "formatter": {
          "type": "string",
          "description": "Model used to format prose into structured JSON"
        }
      },
      "additionalProperties": false
    },
    "personaModels": {
      "type": "object",
      "description": "Per-persona model defaults. Maps personaId to model ID. Overrides models.experts for specific personas.",
      "additionalProperties": {
        "type": "string",
        "description": "OpenRouter model ID for this persona"
      },
      "examples": [
        {
          "security": "openai/gpt-4o",
          "performance": "anthropic/claude-sonnet-4"
        }
      ]
    },
    "timeouts": {
      "type": "object",
      "description": "Timeout configuration for API calls. Use 'unlimited' for no timeout.",
      "properties": {
        "expertMs": {
          "oneOf": [
            { "type": "integer", "minimum": 10000 },
            { "type": "string", "enum": ["unlimited"] }
          ],
          "description": "Timeout for expert analysis in milliseconds. Use 'unlimited' for no timeout."
        },
        "leadMs": {
          "oneOf": [
            { "type": "integer", "minimum": 30000 },
            { "type": "string", "enum": ["unlimited"] }
          ],
          "description": "Timeout for consolidation phases in milliseconds. Use 'unlimited' for no timeout."
        },
        "formatterMs": {
          "oneOf": [
            { "type": "integer", "minimum": 10000 },
            { "type": "string", "enum": ["unlimited"] }
          ],
          "description": "Timeout for formatting prose in milliseconds. Use 'unlimited' for no timeout."
        }
      },
      "additionalProperties": false
    },
    "maxTokens": {
      "type": "object",
      "description": "Token limits for API calls. Use 'unlimited' for no limit.",
      "properties": {
        "experts": {
          "oneOf": [
            { "type": "integer", "minimum": 100 },
            { "type": "string", "enum": ["unlimited"] }
          ],
          "description": "Max tokens per expert analysis. Use 'unlimited' for no limit."
        },
        "lead": {
          "oneOf": [
            { "type": "integer", "minimum": 100 },
            { "type": "string", "enum": ["unlimited"] }
          ],
          "description": "Max tokens for consolidation phases. Use 'unlimited' for no limit."
        }
      },
      "additionalProperties": false
    },
    "maxDraftPlanLength": {
      "type": "integer",
      "minimum": 1000,
      "description": "Maximum length of draft plan input in characters. The document sent to consult_council should NEVER be summarized by the agent. If the document exceeds this limit, inform the user of their options: increase this limit, split into multiple consultations, or remove non-essential sections."
    },
    "maxHistorySessions": {
      "type": "integer",
      "minimum": 1,
      "maximum": 100,
      "description": "Maximum number of session history entries to retain"
    },
    "databasePath": {
      "type": "string",
      "description": "Path to SQLite database file for session storage"
    },
    "cacheTtlSeconds": {
      "type": "integer",
      "minimum": 60,
      "description": "Cache time-to-live in seconds for persona data"
    },
    "formatter": {
      "type": "object",
      "description": "Formatter service configuration",
      "properties": {
        "maxRetries": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10,
          "description": "Maximum retries for formatting failures"
        }
      },
      "additionalProperties": false
    }
  },
  "required": ["personas"],
  "additionalProperties": false
}